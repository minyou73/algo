# 0에서 9까지 숫자가 적힌 N장의 카드가 주어진다.

# 가장 많은 카드에 적힌 숫자와 카드가 몇 장인지 출력하는 프로그램을 만드시오. 
#  카드 장수가 같을 때는 적힌 숫자가 큰 쪽을 출력한다.

# 4 9 6 7 
#   9
import sys
sys.stdin = open('input.txt')

T = int(input())
for tc in range(1, T+1):
    N = int(input())
    numbers = input()

    a = [0] * 10

    for i in numbers:
        a [int(i)] += 1
    
    num = 0
    card = 0

    for x,y in enumerate(a):
        if >       #카드 장수가 같을 때는 적힌 숫자가 큰 쪽을 출력한다.
        
        num = x
        card = 
        
    print()


############################################################
count()